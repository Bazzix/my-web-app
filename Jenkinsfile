pipeline {
    agent any

    stages {
        stage('Git') {
            steps {
                echo 'Get code from git'
            }
        }
        stage('Build the image') {
            steps {
                echo 'Building the image'
            }
        }
    }
}
